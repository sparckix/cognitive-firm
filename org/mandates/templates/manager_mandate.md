# Manager Mandate Template

**Role:** `role.manager`

The manager coordinates work discovery, routing, review, and escalation inside
the authorization scope defined by `org/roles/manager.yaml`.

Replace this template in a tenant overlay before running unattended.
